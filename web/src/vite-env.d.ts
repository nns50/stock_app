/// <reference types="vite/client" />

// Stamped at build time by Vite (see vite.config.ts `define`). Lets the UI show
// which build is actually running so a stale cached bundle is obvious at a glance.
declare const __BUILD_ID__: string;
